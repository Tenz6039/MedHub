<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.medhub.mapper.OrderDetailMapper">

    <insert id="insertBatch"  useGeneratedKeys="true" keyProperty="id" parameterType="com.medhub.entity.OrderDetail">
        insert into order_detail (name, image, order_id, medicine_id, combo_id, medicine_spec, amount)
        VALUES
            <foreach collection="orderDetailList" item="orderDetail" separator=",">
                (#{orderDetail.name}, #{orderDetail.image}, #{orderDetail.orderId}, #{orderDetail.medicineId}, #{orderDetail.comboId}, #{orderDetail.medicineSpec}, #{orderDetail.amount})
            </foreach>
    </insert>
</mapper>
